import javax.swing.*;
import javax.xml.crypto.Data;
import java.net.MalformedURLException;
import java.nio.file.Paths;

public class Main2 {

    public static void main(String[] args) {

        /*
        String windowsPath = "C:\\Users\\natal\\OneDrive\\Documentos\\Prueba.txt";

        Data data = null;

        String text = "Esto es un texto de prueba";

        //Write text file
        data = new Data(text);
        try {
            data.write(Paths.get(windowsPath).toUri().toURL(), true);
        }
        catch (MalformedURLException e) {
            throw new RuntimeException(e);
        }

        //Read text file
        data = null;
        try {
            data = new Data(data.write(Paths.get(windowsPath).toUri().toURL(), true));
        }
        catch (MalformedURLException e) {
            throw new RuntimeException(e);
        }

        if (data!= null){
            String newText = data.toText();
            JOptionPane.showMessageDialog(null, newText);
        }

         */



    }
}
